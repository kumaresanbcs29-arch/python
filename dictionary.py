from enum import nonmember


def acc(x):
    m=input("enter element to be accessed:")
    k=m.keys()
    for i in range (len(x)):
        if m in  k:
            print("ELEMENT IS PRESENT")
            return 0
        else:
            print ("not found")
            return 0
        return 0
    def addition (x):5
        m=input("enter element to be added:")
        for i in range (m):
            a=input("key")
            b=input("value")
            x[a]=b
        def date(x):
            m=input("enter element to be added:")
            if len (x)==0:
            m=input("enter element to be deleted:")
            def

